// Import the User model
const User = require('./User');
// Import the Pet model
const Pet = require('./Pet');
// Import the Donation model
const Donation = require('./Donation');

// Export the User, Pet, and Donation models for use in other parts of the application
module.exports = { User, Pet, Donation };
